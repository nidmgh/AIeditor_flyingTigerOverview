# Build outputs and intermediates (regenerated by the build scripts)
tmp/
**/tmp/
output/
**/output/
stage/
**/stage/

# OS noise
.DS_Store
*.swp

# Local-only hand-off notes (never publish — contains author-local paths)
HANDOVER.md

# Blog media — web-optimized video re-encodes, staged locally and deployed via
# the blog repo's assets/. Too large for git history (tens of MB each).
blog-assets/
