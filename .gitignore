# Build outputs and intermediates (regenerated by the build scripts)
tmp/
**/tmp/
output/
**/output/
stage/
**/stage/

# OS noise
.DS_Store
*.swp

# Local-only hand-off notes (never publish — contains author-local paths)
HANDOVER.md

# Blog media is tracked here (heroes, pipeline diagrams, logos, web clips),
# except the full-resolution chapter render which is too large for git history.
blog-assets/flying-tigers-chapter01.mp4
